fastapi>=0.115,<1.0
uvicorn>=0.32,<1.0
# starlette.middleware.sessions.SessionMiddleware imports itsdangerous at
# module scope. starlette/fastapi don't pull it in, so we declare it here
# explicitly — without this, a clean install (e.g. inside the docker image)
# fails to import api.main even though `pip install fastapi` succeeded.
itsdangerous>=2.2,<3.0
psycopg[binary]>=3.2,<4.0
psycopg-pool>=3.2,<4.0
pyyaml>=6.0,<7.0
bcrypt>=4.2,<5.0
python-multipart>=0.0.20,<1.0
